#include "Calculator.hpp"

int Calculator::Summation(int x, int y)
{
   return (x + y);
}

int Calculator::Difference(int x, int y)
{
   return (x - y);
}

int Calculator::Multiplication(int x, int y)
{
   return (x * y);
}

int Calculator::Division(int x, int y)
{
   return (x / y);
}

